# VOKO 0.5.6：网关认证、恢复与 Hermes 修复

状态：已于 2026-09-11（UTC）发布 npm 与 GitHub Release，发布源码为 `9308b343603497fd3bb152b4754a53cb605773db`。

## 发布范围

- 统一 Hermes、OpenClaw Gateway 启动与认证就绪判定，遵循所选运行时，显示启动失败原因。
- 修复 Hermes profile 认证、精确路由、配置更新和网关恢复边界；更新 YAML 时保留无关配置并创建备份。
- 请求提交后若 Hermes Provider 停止或被替换，迟到回复记为 `outcome_unknown`，不再误报完成，也不自动降级重发。
- Hermes 注册优先读取本地 profile 配置，避免反复启动原生 CLI 带来的延迟；本地目录不可识别时保留 CLI 发现路径。
- 恢复 WorkBuddy 运行时验证，完善 Provider 启动诊断及提交前后的结果分类。
- 用 Node.js 内置 crypto 替换废弃的 crypto-js，保留 IM AES-CBC/PKCS7 文本线格式；不是 E2EE 协议升级。
- 将间接依赖 Hono 的锁定版本更新为 4.13.7，修复依赖审计发现的问题。

## 升级与兼容性

Node.js 要求保持 `>=22.5.0`。本次只发布 `@voko/lite`，不升级独立 Chatroom、AgentDID 或 Hermes 本体，也不部署服务端数据库迁移。

可执行 `npm install -g @voko/lite@0.5.6`，或运行 `voko update`。升级前备份本地数据，等待已提交请求结束，再按现有运行方式重启 Lite；安装新包不会让已运行进程自动加载新代码。结果未知时先核对原始任务和消息记录，不要重复派发。

profile 被发现不代表模型凭据、HTTP 认证或 IM 连接已经就绪。网关检查需要实际认证通过；模型成功回复也不等于访客已收到 IM 消息。参见 [Hermes 指南](../providers/hermes.md)。协作功能仍受 [0.5.5 服务端要求](0.5.5.md#服务端与兼容性)约束。

## 验证范围

发布准备前的定向验证：

- macOS 与 Ubuntu 各 208 项通过；Windows 206 项通过、2 项 POSIX 符号链接测试跳过。
- macOS 额外 Dispatcher/CLI Provider 回归 87 项通过。
- macOS 使用修复后的 Hermes HTTP Provider 完成一次真实模型请求，约 5.6 秒返回预期内容；没有将此测试视为外部 IM 投递验收。
- Windows 本地 profile 发现连续 100 次约 470 毫秒，与原生 CLI 的 profile 名称及默认选择一致；完整注册测试通过。

这些是定向证据，不能替代最终提交的完整发布门禁，也不代表所有 Hermes 版本、模型和工具权限组合都已实测。最终发布提交的完整门禁、安全扫描和产物验证均已通过，记录见下方。

## 发布流程

按 [发布流程](../../RELEASING.md)与[发布清单](../../RELEASE_CHECKLIST.md)执行：版本 PR 通过必需检查后合并，等待 main 完整矩阵并运行 `release:preflight`；触发 **Publish npm**，版本 `0.5.6`、`prerelease=false`。审核准备产物后审批 `npm-production`，由 OIDC 发布并核对 npm tarball SHA-512、provenance、标签和 GitHub Release。

npm 上传成功但暂不可读时等待验证，不重复上传同一版本。

## 正式发布记录

- npm：[@voko/lite 0.5.6](https://www.npmjs.com/package/@voko/lite/v/0.5.6)。
- GitHub：[v0.5.6](https://github.com/laoyudashu/voko/releases/tag/v0.5.6)，非草稿、非预发布，标签指向上述发布源码。
- [PR #73](https://github.com/laoyudashu/voko/pull/73)、[main CI](https://github.com/laoyudashu/voko/actions/runs/34556870704) 和 [CodeQL](https://github.com/laoyudashu/voko/actions/runs/34556870697) 已通过。main 覆盖 Node 22/24 三系统及 Chromium、Firefox、WebKit。
- [发布工作流](https://github.com/laoyudashu/voko/actions/runs/34557793119)完成发布门禁、Chromium、确切产物扫描、OIDC 发布、registry 校验及标签/Release 创建。
- 本地 `release:preflight` 和 `release:verify` 通过；独立下载的 npm tarball 与准备产物 SHA-512 一致，provenance 的来源提交、工作流和产物摘要匹配。
- 产物 337 个文件、3,809,371 字节；SHA-512：`sha512-Ko9yt6uH7PcBt3swD5b6z26EVjf9SRjXpsBEFrBHhfVBYkZqHaAGB1ErKc9wsvnLj6nelnzufaJ64z3POimjEQ==`。
- 发布验证修正了测试中的认证前提与 Windows 8.3 路径比较；真实 Windows 短路径回归 7 项通过。main 首次 Windows Node 22 的 Python 启动超时经日志审查后只重跑失败作业，未改断言，最终通过；本地 Windows 使用真实 Python 的 OpenHands 安全回归 8 项通过。
- 两条 CodeQL 401 恢复分支误报按精确指纹记录及复核，真实 HTTP 测试验证重新认证仍必需、HTTP 500 和断流不触发重发；没有关闭规则或放宽运行时认证。
- 本次未升级或重启用户现有 Lite/Hermes 服务；npm 发布不等于运行中的旧进程已加载新代码。
