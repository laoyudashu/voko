# VOKO 0.5.3 发布说明

状态：已于 2026-09-06（UTC；洛杉矶时间 2026-09-05）正式发布。[npm @voko/lite@0.5.3](https://www.npmjs.com/package/@voko/lite/v/0.5.3) 与 [GitHub Release v0.5.3](https://github.com/laoyudashu/voko/releases/tag/v0.5.3) 对应提交 `dc11dfe7a06862686787b8e7b7a5520fdcbe144c`。发布核验时 npm `latest` 为 0.5.3。

## 主要变化

- **OpenClaw 版本兼容**：读取旧 `agents.list` 与新 `agents.entries`；未知版本先检查 CLI 参数契约；Gateway 版本取自认证握手。同一状态目录的 CLI 调用排队，避免并发争用。仅当自身启动的 Gateway 明确要求迁移后重启时恢复一次，两次尝试共用 90 秒预算，启动错误提供脱敏诊断。包版本与文件身份使用同一已打开文件读取，避免升级替换期间的检查/读取竞争。
- **Codex 访客权限**：根据实际 CLI 参数和原生沙箱读写检查开放权限，而不是按版本号推断。运行入口变化后重新验证；首轮和恢复调用保持相同沙箱及审批语义。失败时显示阶段、退出码和脱敏摘要，不绕过沙箱。
- **通信与安全修复**：加强本地 Web 会话与事件流授权、MCP 输入检查和目录访问拒绝后的加密投递边界；修复离线同步断点、缺口与续传，以及终态回执恢复。保留执行证据，结果未知时不自动重投任务。
- **其他 Provider 修复**：改进 ACP 连接失败分类、WorkBuddy 启动诊断与 loopback 隔离、OpenHands 安全钩子失败处理、Windows Copilot 发现与进程归属判断、Aider 回复尾部过滤。
- **升级与发布**：统一使用官方 npm registry 发现和安装更新；本地页面显示可升级的确切版本。发布扫描覆盖实际 npm tarball，并拒绝路径别名及特殊文件等不安全输入。

## 兼容性与已知限制

兼容机制覆盖旧版、当前版和未知版本的能力判定，不代表所有发行版或所有宿主环境均已验收。

- OpenClaw 参数/握手锚点为 2026.6.1、2026.7.1-2、2026.9.2。macOS 2026.9.2 和 Windows 2026.7.1-2 的 CLI/WS 模型首轮与会话恢复通过；Ubuntu 测试入口缺少 DeepSeek 凭据，模型执行被阻塞。OpenClaw 原生权限尚未绑定为 VOKO 可验证的逐回合控制，因此没有新增权限开关。
- Codex macOS 0.153.4 的沙箱、模型首轮及会话恢复通过；Ubuntu 0.148.0 被 bwrap 初始化权限拒绝，Windows 0.148.0 的 workspace-write 检查仍拒绝工作区写入，两个环境的访客 Provider 保持提交前阻止。旧式平台沙箱子命令仅有模拟契约覆盖。
- 上述窄范围 Provider 真机验证不包含附件、访客 Web、E2EE 或 IM 投递验收，不能用模型回复代替完整投递证明。

详细证据与操作说明：[OpenClaw 兼容性](../providers/openclaw-compatibility.md)、[Codex 兼容性](../providers/codex-compatibility.md)。

## 升级与回退

可运行 `npm install -g @voko/lite@0.5.3`，或使用 `voko update` 安装 registry 当前稳定版本。需要 Node.js >=22.5.0。

升级后在“访客安全与权限”重新检测，查看当前宿主环境的实际结果。该版本不会替用户升级 Codex/OpenClaw，也不自动把 OpenClaw 原生配置改成统一权限模板。OpenClaw 已保存的补充提示语无需迁移；升级前未提交的 preflight 应重新获取。

若需回退 VOKO，可安装 `@voko/lite@0.5.2`；这不会回退上游 Agent 状态或撤销已执行任务，也会失去本版本的安全与可靠性修复。

## 发布准备验证

2026-09-05 本地 macOS / Node 26.7.0：完整 `release:gate` 通过，1642 项测试中 1640 通过、2 项平台条件跳过，类型检查、i18n、覆盖率和生产依赖审计通过；Gitleaks 与 CodeQL extended 门禁通过。

Chromium 39 项浏览器测试通过，测试基础设施 6 项回归通过。发布检查还修复了浏览器测试目录请求误连公网的问题：仅在测试子进程内注入本地 API 地址，不改写构建配置、不关闭 E2EE；模拟目录明确声明旧式测试收件人不支持 E2EE，并以真实浏览器验证目录 401 时 IM 发送计数不增加。

CodeQL 的 manifest 检查/读取竞争已由失败回归确认并修复；挑战时间戳拒绝条件为误报，按规则、文件与精确指纹记录理由，未删除安全校验。首轮 GitHub CI 还发现 Gateway 测试依赖开发机已有配置，已改为独立临时配置；指定不存在的宿主配置后，相关 29 项测试通过。Windows tar 清单的 CRLF 解析已用失败回归确认并修复；清单采用严格 UTF-8 解码，系统代码页无法无损表达名称时在解包前拒绝扫描，避免使用替换字符校验路径；命令包装进程已启动后的失败仍保持结果未知，测试改为区分该路径与真正的执行前失败。路由回归等待实际状态，不再依赖 25ms 固定延时。GitHub PR 的 Node 22 三系统、Rust E2EE 和 CodeQL 结果以对应 Actions 为准；此处本地通过不代替远端门禁或 main 合并后的完整矩阵。

## 正式发布验证

- [PR #59](https://github.com/laoyudashu/voko/pull/59) 已合并；[main 完整 CI](https://github.com/laoyudashu/voko/actions/runs/34010551045) 的 Node 22/24 三系统、三浏览器及 E2EE 门禁通过，[CodeQL](https://github.com/laoyudashu/voko/actions/runs/34010551117) 通过。
- 合并提交本地 `release:preflight` 通过：1645 项测试中 1643 通过、2 项平台条件跳过；类型、i18n、覆盖率、生产依赖审计、Gitleaks、CodeQL 和打包门禁通过。
- [Publish npm 运行](https://github.com/laoyudashu/voko/actions/runs/34010815062) 的准备门禁、Chromium 和实际产物扫描通过。审批不可变产物后通过 OIDC 发布；npm 接受包后提示仍在处理，工作流约 45 秒的 registry 核验窗口超时，因此运行显示失败，自动 tag/Release 步骤跳过。
- 等待 npm 公开包可下载后，确认 `latest=0.5.3`，正式 tarball 与审批产物逐字节一致，provenance 指向该运行与上述提交。未重新发布 npm；核验后仅补建原提交的 `v0.5.3` 和正式 GitHub Release。
- `npm run release:verify` 通过，升级发现返回 0.5.3。包共 326 个文件、3,754,583 字节，SHA-256 为 `9bdb38cdfb3b68c9036546400e17d90f4381a7604a7c69bf699e64a3f36a3d18`。

后续发布继续遵循[发布清单](../../RELEASE_CHECKLIST.md)。npm 已接受但仍在处理时，应先等待并核对 registry；不要因后置核验超时重发相同版本或将 tag 指向后来移动的 main。
