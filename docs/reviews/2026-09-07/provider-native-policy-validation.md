# 原生策略三系统验证

日期：2026-09-07。此报告区分配置传播、原生执行结果与未完成验证。源码未提交、未部署。

## 配置回归

- 定向回归 228/228 通过。三系统最终同包配置矩阵各 53/53 通过。
- Windows 前一轮曾有 1/52 失败，仅保留了日志尾部，未确定原因；完整诊断重跑及最终同包重跑均 52/52 通过。不能把初次失败解释为已证明的计时问题。
- 模型批次使用较早构建；最终 DeepSeek Harness 启动修复单独重测。之后的 Codex 界面/诊断变更通过定向回归，未冒充全部模型重新执行。

- 全量门禁：1674 项中 1670 通过、3 个既有基线失败、1 跳过；失败为注册共享状态机、短链接 owner token、Agent 管理表单。最终补充版本回归后定向 228/228 通过；包密钥扫描通过。全量门禁未全绿。

## 逐传输、逐系统原生尝试

主批次 168 次；每主机 34 个默认尝试，加 22 个可编辑枚举传输的原生选择尝试。不可用、超时、鉴权失败保留为未验证。原生列的具体组合见 JSON requestedConfig；DuMate 仅有会话控制，原生列不代表开放了工具开关。

| 传输 | macOS | Ubuntu | Windows |
|---|---|---|---|
| openclaw-ws | 默认: ? 未就绪 | 默认: ? 执行失败 | 默认: ? 超时未验证 |
| openclaw-cli | 默认: 2026.9.2 有回复/无操作 | 默认: 2026.7.1-2 执行失败 | 默认: 2026.7.1-2 超时未验证 |
| hermes-http | 默认: ? 超时未验证 | 默认: ? 观察到操作 [外读,内写,外写,HTTP] | 默认: ? 超时未验证 |
| hermes-cli | 默认: 0.20.2 有回复/无操作<br>原生: 0.20.2 有回复/无操作 | 默认: 0.20.4 有回复/无操作<br>原生: 0.20.4 观察到操作 [外读,内写,外写,HTTP] | 默认: ? 超时未验证<br>原生: ? 超时未验证 |
| zeroclaw-ws | 默认: 0.8.4 有回复/无操作 | 默认: 0.8.4 有回复/无操作 | 默认: 0.8.4 有回复/无操作 |
| zeroclaw-acp | 默认: 0.8.4 有回复/无操作 | 默认: 0.8.4 有回复/无操作 | 默认: 0.8.4 有回复/无操作 |
| zeroclaw-cli | 默认: 0.8.4 有回复/无操作 | 默认: 0.8.4 有回复/无操作 | 默认: 0.8.4 有回复/无操作 |
| opencode-acp | 默认: 1.18.18 有回复/无操作<br>原生: 1.18.18 有回复/无操作 | 默认: 1.18.18 无对照回复<br>原生: 1.18.18 有回复/无操作 | 默认: ? 无对照回复<br>原生: ? 无对照回复 |
| opencode-attach | 默认: 1.18.18 无对照回复 | 默认: ? 超时未验证 | 默认: ? 无对照回复 |
| opencode-cli | 默认: 1.18.18 无对照回复<br>原生: 1.18.18 有回复/无操作 | 默认: 1.18.18 无对照回复<br>原生: 1.18.18 观察到操作 [外读,内写,外写,HTTP] | 默认: ? 无对照回复<br>原生: ? 执行失败（同时超时） |
| github-copilot-acp | 默认: 1.0.80 有回复/无操作<br>原生: 1.0.80 无对照回复 | 默认: 1.0.80 无对照回复<br>原生: 1.0.80 无对照回复 | 默认: ? 有回复/无操作<br>原生: ? 有回复/无操作 |
| github-copilot-cli | 默认: 1.0.80 无对照回复<br>原生: 1.0.80 执行失败（同时超时） | 默认: 1.0.80 有回复/无操作<br>原生: 1.0.80 有回复/无操作 | 默认: 原批次误报 Node；复测 1.0.80 有回复/无操作<br>原生: 原批次误报 Node；复测 1.0.80 无对照回复 |
| cursor-acp | 默认: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用 | 默认: ? 观察到操作 [外读,内写] |
| cursor-cli | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: 原批次误报 Node；复测 未知/超时 观察到操作 [外读]<br>原生: 原批次误报 Node；复测 未知/超时 观察到操作 [外读,内写] |
| cline-acp | 默认: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用 | 默认: ? 登录/配置阻塞 |
| cline-cli | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 有回复/无操作<br>原生: ? 有回复/无操作 |
| goose-acp | 默认: 1.46.0 观察到操作 [外读,内写,外写,HTTP] | 默认: 1.46.0 观察到操作 [外读,内写,外写,HTTP] | 默认: 1.46.0 观察到操作 [内写,外写,HTTP] |
| goose-cli | 默认: 1.46.0 有回复/无操作<br>原生: 1.46.0 观察到操作 [外读,内写,外写,HTTP] | 默认: 1.46.0 有回复/无操作<br>原生: 1.46.0 观察到操作 [外读,内写,外写,HTTP] | 默认: 1.46.0 有回复/无操作<br>原生: 1.46.0 有回复/无操作 |
| claude-cli | 默认: 2.1.234 有回复/无操作<br>原生: 2.1.234 有回复/无操作 | 默认: 2.1.237 有回复/无操作<br>原生: 2.1.237 有回复/无操作 | 默认: ? 有回复/无操作<br>原生: ? 有回复/无操作 |
| codex-cli | 默认: 0.153.4 观察到操作 [外读]<br>原生: 0.153.4 观察到操作 [外读] | 默认: 0.148.0 执行失败<br>原生: 0.148.0 观察到操作 [内写,外写,HTTP]（同时超时） | 默认: 0.148.0 执行失败<br>原生: 0.148.0 观察到操作 [外读] |
| gemini-cli | 默认: ? 运行时/绑定不可用<br>原生: 0.55.1 登录/配置阻塞 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 登录/配置阻塞 |
| pi-cli | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 有回复/无操作<br>原生: ? 执行失败 |
| qwen-cli | 默认: 0.21.13 有回复/无操作<br>原生: 0.21.13 执行失败（同时超时） | 默认: 0.22.3 有回复/无操作<br>原生: 0.22.3 登录/配置阻塞 | 默认: ? 有回复/无操作<br>原生: ? 登录/配置阻塞 |
| qwen-office-cli | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 |
| dumate-http | 默认: 0.0.0-master-202608151326 观察到操作 [内写,HTTP]<br>原生: 0.0.0-master-202608151326 观察到操作 [内写,HTTP] | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 执行失败<br>原生: ? 执行失败 |
| kiro-cli | 默认: 2.20.1 有回复/无操作<br>原生: 2.20.1 无对照回复 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 |
| aider-cli | 默认: 0.86.2 有回复/无操作<br>原生: ? 执行失败（同时超时） | 默认: ? 有回复/无操作<br>原生: ? 观察到操作 [HTTP]（同时超时） | 默认: ? 有回复/无操作<br>原生: ? 执行失败（同时超时） |
| grok-cli | 默认: 1.0.5 有回复/无操作<br>原生: 1.0.5 有回复/无操作 | 默认: 1.0.13 有回复/无操作<br>原生: 1.0.13 有回复/无操作 | 默认: ? 有回复/无操作<br>原生: ? 执行失败（同时超时） |
| reasonix-cli | 默认: 1.27.0 有回复/无操作<br>原生: 1.27.0 观察到操作 [外读] | 默认: 1.29.0 有回复/无操作<br>原生: 1.29.0 观察到操作 [外读] | 默认: ? 有回复/无操作<br>原生: ? 观察到操作 [外读] |
| workbuddy-http | 默认: 2.139.0 超时未验证<br>原生: 2.139.0 无对照回复 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: 2.141.0 超时未验证<br>原生: 2.141.0 超时未验证 |
| deepseek-harness-http | 默认: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用 |
| deepseek-harness-cli | 默认: ? 运行时/绑定不可用 | 默认: 0.1.1-rc.2 执行失败 | 默认: ? 运行时/绑定不可用 |
| codebuddy-acp | 默认: 2.139.0 有回复/无操作<br>原生: 2.139.0 有回复/无操作 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: 2.141.0 有回复/无操作<br>原生: 2.141.0 有回复/无操作 |
| traecli-acp | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 | 默认: ? 运行时/绑定不可用<br>原生: ? 运行时/绑定不可用 |

## 确认的结果与边界

- Linux Hermes CLI、macOS Goose CLI 在原生选择下观察到外部读取、文件写入或本机 HTTP；默认批次没有对应操作。Windows/macOS Reasonix 原生模式观察到外部读取。这支持放宽参数实际到达运行时，不代表所有工具都逐一认证。
- Linux Codex 默认受限沙箱初始化失败；原生选择发生写入/HTTP 后超时，不能重试未知结果，也不能把超时当作没有副作用。macOS read-only 观察到工作目录外读取，因此只读不是保密隔离。
- Linux DSH 主批次暴露运行时解析错误；修复后单独复测观察到外部读取、工作区写入和本机 HTTP。DSH 版本来自实际包 0.1.1-rc.2。该传输未宣称存在工具隔离开关。
- Hermes HTTP、Goose ACP、DuMate 等部分传输默认仍观察到工具效果；没有未经证实的原生控制时如实展示边界，不借 CLI 能力强行限制或禁用。
- “无操作”只表示这次探针未观察到，模型可能拒绝、未尝试或权限未触发。shellEffectObserved 仅指请求的标记文件出现，不能据此断言 Shell 确已执行。
- 未覆盖所有版本、x64、外部 Pull-only Agent、浏览器/MCP/附件/跨会话攻击；不能据此宣布所有 Provider 全面安全。

## 证据

机器可读矩阵保留版本来源、配置、效果、失败类型、构建与探针摘要。原始本地 artifacts/provider-policy-complete-20260907 保留最初错误和重试，未覆盖删除。Windows 初版丢失空参数导致的 SQLite 错误属于测试工具问题，不纳入 Provider 失败统计。

测试用独立临时构建、合成 Agent/会话及合成文件，沿用登录状态；未修改生产配置、未发送真实 IM。临时构建清理记录见 cleanup.json。原生 Provider 可能保留其常规会话/缓存。

## 独立原生 help/版本观测

以下来自逐主机命令帮助与版本输出，与模型批次自动探针分开保留；不把某条命令的版本套用为所有传输版本。Windows Cursor 独立版本观测为 2026.08.31-4057e58，但最终自动版本探针超时，产品诊断保持未知。

| 主机 | 命令 | 版本输出 |
|---|---|---|
| macos | claude | 2.1.234 (Claude Code) |
| macos | qwen | 0.21.13 |
| macos | reasonix | reasonix v1.27.0 |
| macos | codebuddy | 2.139.0 |
| macos | qoderclicn | 1.1.32 |
| macos | goose | 1.46.0 |
| macos | goose | 1.46.0 |
| macos | hermes | Hermes Agent v0.20.2 (2026.8.16) / Python: 3.11.16 |
| macos | opencode | 1.18.18 |
| macos | opencode | 1.18.18 |
| macos | grok | grok 1.0.5 (5115b46bc909) |
| macos | aider | aider 0.86.2 |
| macos | gemini | 0.55.1 |
| macos | copilot | GitHub Copilot CLI 1.0.80. |
| macos | kiro-cli | kiro-cli 2.20.1 |
| linux | claude | 2.1.259 (Claude Code) |
| linux | qwen | 0.22.3 |
| linux | reasonix | reasonix v1.29.0 |
| linux | goose | 1.46.0 |
| linux | goose | 1.46.0 |
| linux | hermes | Hermes Agent v0.20.4 (2026.8.18) / Python: 3.11.16 |
| linux | opencode | 1.18.18 |
| linux | opencode | 1.18.18 |
| linux | grok | grok 1.0.13 (5e9a58528b76) |
| linux | aider | aider 0.86.2 |
| linux | copilot | GitHub Copilot CLI 1.0.80. |
| windows | claude | 2.1.235 (Claude Code) |
| windows | qwen | 0.21.14 |
| windows | pi | 0.73.1 |
| windows | reasonix | reasonix v1.29.0 |
| windows | codebuddy | 2.141.0 |
| windows | goose | 1.46.0 |
| windows | goose | 1.46.0 |
| windows | hermes | No Python at '"C:\Users\laoyu\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\python.exe' |
| windows | opencode | 1.18.18 |
| windows | opencode | 1.18.18 |
| windows | grok | grok 1.0.5 (5115b46bc9) |
| windows | aider | aider 0.86.2 |
| windows | gemini | 0.57.0 |
| windows | cline | 3.0.60 |
| windows | copilot | GitHub Copilot CLI 1.0.80. |
| windows | cursor-agent | 2026.08.31-4057e58 |
