# WorkBuddy 回环投递隔离修复：2026-09-05

## 修复与验证

提交 `dcc7f7b` 修复上一轮确认的回环回复误入 IM 投递问题。原 `runLoopbackTest` 通过业务 `push` 发出 agent.reply 和 accepted/completed 事件，且修改 lastDelivery；实际 Windows 日志因此对虚拟 visitor voko-loopback 发起收件人解析并返回 PEER_NOT_FOUND。

新增三项测试在原实现全部失败。修复通过内部结果回调接收回环答案，继续使用绑定实例、既有服务启动和 ACP 请求实现；回环不进入业务回复、生命周期、活动业务 turn 或最后业务投递状态。未采用全局静音或按测试标记前缀丢弃消息，因此同一 Provider 的正常并发消息仍可发送，包括形似挑战标记的业务 turnId。失败仍抛出原分类错误，并断开该次 ACP 连接。

测试覆盖成功时无业务副作用、空回复失败时清理连接且不覆盖业务状态、回环挂起期间正常业务投递。测试编写期间一个初版并发夹具误用了与回环完全相同的 turnId，触发旧实现去重并等待；只停止该测试自己的两个 Node 进程，改用独立的挑战形状业务 ID 后，正式红灯记录为 3 项全部失败。

修复后相关 23 项通过。完整 release:gate:code：1574 项通过、2 项既有跳过、0 失败；构建、类型、i18n、覆盖率基线和源码包扫描通过。最终差异检查通过。

## 同包三端部署

本地 @voko/lite 0.5.2 候选，未发布 npm registry。

- tarball SHA-256：`21d2593ad49f599c14dc9931755ade25e78748fa761369c2b96dc8c2e9b82a70`。
- 三端运行构建摘要：`bec1436a5a179580d19348b7955a97e2a3a4b6aa33f9fb95ee01d9d9af302805`，312 个构建文件。
- Mac PID 46965、Linux PID 257660、Windows PID 12768 均 READY，IM 分别 17/17、12/12、21/21；buildMismatch=false。
- 三端部署前均完成备份；实际 tarball 扫描 320 个文本文件无发现。
- 安装包上的 3 项新增测试在 Mac、Linux、Windows 分别全部通过。
- Mac DuMate、千问办公 o9 在新进程中重新完成实际回环验证。

17:04:14.695 UTC，Windows WorkBuddy 生产实例回环挑战匹配成功。随后当前候选启动窗口日志没有 voko-loopback 误投递或 PEER_NOT_FOUND。与复现测试一起支持该隔离缺陷已修复；回环成功不证明实际网页对话及原生间歇性空回复已修复。

## 网页回归尚未完成

17:01:33–17:04:24 UTC，从原 macOS Chrome VOKO 页面向 24 个原有身份发送本候选 R1 标记：全部 17 个 Mac、7 个 Linux，均保存发送后的实际 DOM 标记。Linux OpenCode 当前标记后的正确答案 15 已在实际页面确认。

随后浏览器调试操作超时。读取原标签页确认它仍在原会话，没有重复发送；控制连接重建后浏览器 ID 变化、调试连接未附着。进一步用 CUA 原生应用接口检查，工具明确报告 Mac 已锁屏且自动解锁失败，已请求用户手动解锁。没有终止 Chrome、关闭用户标签页、清除会话或绕过锁屏。

锁屏前已发送的 24 条在后台有 23 条 generated/delivered 和 1 条自动投递未启用，但仅已实际看到的 Linux OpenCode 答案计入新候选网页成功数。目前为 1/50 已确认、23 条待网页复核、26 个待首测。上一候选 44/50 完整保存在历史，不能沿用为新包结果。剩余五个 Linux 身份及全部 21 个 Windows 身份尚未发送新候选消息。

待解锁后继续原网页，先核对已发送的标记，再对剩余身份各发送一条新候选消息。不得将未执行的网页验收改成 CLI 或日志验收。原六项配置/原生故障仍保留；本修复不代表整个优化目标完成。

证据：`artifacts/production-remediation-loopback-20260905/` 保存包、备份、部署、摘要、三端安装测试、真实回环、日志和 browser-sent.json；本地红绿测试与完整门禁日志位于 `/tmp/voko-loopback-*.log`。execution.json 保存当前候选及未完成网页步骤。
